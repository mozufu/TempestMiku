# TODO

No standalone TODO checklist is active.

Use `ROADMAP.md` as the canonical source for execution order, deferred SDK namespace placement,
and milestone acceptance checks. The previous SDK contract checklist was folded back into
`ROADMAP.md` and `docs/design/core/07-host-sdk.md`.
